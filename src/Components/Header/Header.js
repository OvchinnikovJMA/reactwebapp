import React from 'react';
import './Header.scss';

const Header = () => {
    return(
        <nav className="navbar sticky-top">
            <header className="navbar-brand">Ovchinnikov Web App</header>
        </nav>
    )
}

export default Header